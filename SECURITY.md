# ReminNote 安全说明

## 当前边界

P0/P1 开发阶段只允许使用本地开发数据和 Mock/测试数据。请不要把 token、密码、真实数据库、生产配置或个人数据提交到仓库。

## 报告安全问题

如果发现可能导致凭据泄露、未授权数据访问、数据丢失或远程代码执行的问题，请不要创建公开 Issue。待 GitHub 私有仓库配置完成后，优先使用 GitHub Private Vulnerability Reporting；也可以直接联系仓库维护者并提供：

- 受影响的提交或版本；
- 可复现步骤和最小样例；
- 影响范围；
- 建议的缓解措施（如有）。

请在报告中删除真实凭据和个人数据。若凭据已经泄露，应立即在对应服务中撤销并轮换，而不是只删除 Git 历史中的文件。

## 开发安全要求

- 开发数据必须位于 `.devdata/`，并遵守 `.gitignore`。
- P0 不访问网络 Provider、生产数据库或真实 Token。
- Schema、migration、IPC、加密和更新器变更必须经过额外审查。

