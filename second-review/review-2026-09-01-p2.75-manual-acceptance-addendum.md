# ReminNote P2.75 二审人工验收补充记录

记录日期：2026-09-01

记录类型：对既有 `second-review/review-2026-09-01-p2.75-second-review.md` 的新增补充记录。

限制：本文件不改写、覆盖或删除任何既有 `reviews/`、`second-review/` 材料；只记录当前集成提交上新出现的验证证据。所有验证均应使用隔离 clone/profile/root，不得触碰 `D:\Anime\.devdata\reminnote.sqlite`。

## 1. 审查范围、证据边界与提交图

本记录覆盖当前集成 worktree `C:\Users\EMT\.codex\worktrees\8f51\Anime` 的 HEAD：

```text
afc9a244136abfb3da7d3ef3c9ff0aecf290c97b  强化 P2.75 恢复状态校验
```

相关基线和实现关系：

```text
3ab342bb6cb0f8b375c60c05b3dcec7831c7400b  P2.5
  |
37e01232aec1e4f34d1adda33a672622a36f6d18  P2.75-00 契约冻结
  |\
  | +-- eff17293d691ffe654f618581401ec09562687c4  P2.75-01 独立窗口
  | +-- bb3d167912655d84870a28868b7a0657259c57dd  P2.75-02 独立窗口
  | +-- 3ba65483b5f813bc091a477806ebb2bd9e63c2ff  P2.75-03 独立窗口
  |
  +-- 4bd6247661158d75665b0cfc012d591b6de301e6  既有二审报告
      -> 883ae89238ba6eebbeb7bbcd7519ae98f5b5299a  集成 01
      -> 4a18223bf7b1f59c634c5357bc1f5b625caa9dfc  集成 02
      -> 02911946b23d4e20af638ce9838bb5899b363222  集成 03
      -> ba59a2a84214cc55c58ebc39ea0d88e0181cd717  P2.75-04 集成
      -> 7382df903a5f7c562d3dbef54b90126d648fa0d5  promote/verify 接线
      -> c33ca2aea9e842b253c67653a5c09830640a978c  恢复/重启门禁
      -> afc9a244136abfb3da7d3ef3c9ff0aecf290c97b  状态校验强化
```

证据边界：本记录中的 Windows 命令输出由用户在隔离目录中执行并粘贴；本记录没有把用户的粘贴结果冒充为本审查进程在用户桌面上的独立观测。用户最后明确确认“我确定没问题了”，记为用户确认的人工验收结果，不等同于带姓名、版本和环境签名的审计签字。

本轮使用的隔离目录为：

- 当前目标 clone：`C:\Users\EMT\Desktop\ReminNote-P275-Manual-20260901`
- P2 基线 clone：`C:\Users\EMT\Desktop\ReminNote-P275-Manual-P2-20260901`
- 当前目标 clone 为 `afc9a244136abfb3da7d3ef3c9ff0aecf290c97b`；P2 基线为 `aeedd36dcacce630ea6939aacb8977c3d70fb7d4`
- 命令 transcript 中未出现对正式 `D:\Anime` 数据库的读写、删除或覆盖操作；正式库保护仍以命令范围和用户提供的执行记录为证据。

## 2. 当前集成代码的静态接线证据

以下是当前 HEAD 的代码定位，证明本次手工流程运行的是集成后的生产接线，而不是只运行某个独立窗口的 fake fixture：

- `src/windows/ReminNote.Agent/Runtime/AgentMigrationStartup.cs:15-26` 定义 P2 到 P2.5 的 source/target migration plan；`:178-191` 将 backup、quiescence、lock、forward applier、Candidate sidecar finalizer、production verifier、atomic promoter 和 state port 接入 production runner。
- `src/windows/ReminNote.Bootstrap/Program.cs:109-135` 对 recovery status 做只读输出，对 restore 通过 Agent 进程协调；`:201-220` 输出状态、backup/manifest/hash、Candidate 和 next action 字段。
- `src/windows/ReminNote.Agent/Runtime/P275RecoveryActor.cs:28-61` 是 Agent 侧唯一恢复写入 actor，并把恢复数据库工作委托给 Infrastructure recovery service；失败统一映射到稳定 failure code。
- `src/windows/ReminNote.Infrastructure/Persistence/P275/P275RecoveryService.cs:702-790` 执行 atomic promote、`promote-unknown` fail-closed 和 promote 后再次 Verify；`:792-810` 只有 Ready marker 写成功后才结束成功路径。
- `src/windows/ReminNote.Infrastructure/Persistence/P275/P275RecoveryStatus.cs:281-310` 对 manifest 固定字段做 exact-schema 检查；`:312-350` bounded 读取；`:353-390` 读取 backup hash 并在不匹配/不可读时返回非成功状态。

这些静态证据只证明接线和安全边界可见，不单独证明所有故障窗口已经被验证。

## 3. 用户提供的隔离验证结果

### P275-MANUAL-001：P2 基线升级到当前 P2.5 target，关键数据保留

- 裁决：`accept`（限于本隔离 clone 和本条成功路径）。
- 严重度：`Info`。
- 是否阻塞 P2.75：否；不能单独解除 P2.75-04 的全部验收门槛。
- 输入：P2 基线数据库 SHA-256 为 `2A7E8197BD4134A4BA28DD8E118E857B7E03BB5CB6EDF4173BE861491908D284`，复制到当前隔离 clone 后启动 Bootstrap。
- 结果：首次 Bootstrap 正常启动并正常关闭；`recovery-status` 返回 `mode=Ready`、`state=READY`、`ready=true`、`writable=true`、`recoveryRequired=false`、空 `failureCode`；`backupHashStatus=Matches`、`manifestStatus=Matches`，备份长度 `65536` 字节。
- 结果：迁移后的 harness 查询仍有 3 条种子 Task，完成项的 `COMPLETED` 和备注保留，ANYTIME 项保留，跨午夜 RANGE 项保留。
- 主要 artifact：`migration-20260828130000_P2ContinuationDeleteBoundary-20260831090000_P25StorageConsistency-20260901T063655614Z-01a05baf-600d-7d4e-94ae-16aaecad530d.sqlite`。
- 该条证据证明了当前集成产物的真实 SQLite 成功迁移和关键数据保留；不证明 P1→P2→P2.5/P3 全链升级。

### P275-MANUAL-002：目标版本重启幂等与 Active DB 保持不变

- 裁决：`accept`（限于当前隔离 clone）。
- 严重度：`Info`。
- 是否阻塞 P2.75：否。
- 结果：第二次正常 Bootstrap 启动并正常关闭；`ActiveHashUnchanged=True`，backup 文件数量 `2→2`，recovery history 文件数量 `3→3`。
- 结论：未观察到目标版本重复启动再次生成迁移备份或改变 Active DB 的行为。

### P275-MANUAL-003：损坏 backup 的恢复拒绝与启动门禁

- 裁决：`accept`。
- 严重度：`Major`（本条安全失败行为通过）。
- 是否阻塞 P2.75：否；仍需其他故障注入覆盖。
- 做法：只在隔离 clone 的唯一 backup artifact 上追加一个字节，随后执行 `--recovery-restore <artifact> --confirm`；测试结束后恢复原 artifact bytes。
- 结果：status 识别为 `mode=RecoveryRequired`、`ready=false`、`writable=false`、`recoveryRequired=true`，并报告 `failureCode=migration.recovery_backup_invalid`、`backupHashStatus=Mismatched`、`manifestStatus=Mismatched`。
- 结果：坏 backup restore 退出码为 `1`；随后普通 Bootstrap 输出 `Agent startup was blocked by the migration gate; Main/Widget were not started.`，退出码为 `1`，`MainProcessDelta=0`。
- 结果：Active hash 保持不变；失败恢复不被当作成功，未观察到覆盖 Active 的行为。

### P275-MANUAL-004：已验证 backup 的恢复成功

- 裁决：`accept`（限于本次已验证、同一 profile root 的 known-good artifact）。
- 严重度：`Info`。
- 是否阻塞 P2.75：否；不能替代 promote-unknown、进程崩溃和权限/空间故障注入。
- 结果：在同一个 `C:\Users\EMT\Desktop\ReminNote-P275-Manual-20260901` profile root 中恢复原始 backup，`GoodRestoreExit=0`；恢复后 status 回到 Ready；harness 仍查询到原 3 条 Task。
- 结果：恢复过程保留了 history generation；多次恢复后的 `HistoryCount=12` 是旧 Active generation 被保留的结果，不作为失败证据。
- 结论：最小 recovery actor 成功路径已获得隔离真实进程证据，且恢复成功后数据可读。

## 4. 证据分层裁决

| 证据层级 | 当前结果 | 裁决边界 |
|---|---|---|
| 静态代码 | 当前 HEAD 的 runner、status、recovery actor、promote 后 Verify 接线可定位 | 不能替代故障窗口验证 |
| 专项单测 | 本轮既有记录中的 P275-01/02/03 专项测试分别通过 | 仍需注意独立 fixture 与集成 SQLite 行为不是同一证据 |
| Release 构建 | 用户确认隔离 clone Release 构建成功 | 本记录按用户提供结果记录；未把构建输出扩写为新的独立观测 |
| 隔离 CLI/clone smoke | P2→P2.5 成功迁移、status、关键数据、幂等重启、坏 backup 拒绝、启动阻断、好 backup 恢复均有 transcript | 证明当前隔离环境的真实进程路径，不代表所有 Windows 部署环境 |
| 真实进程 | Bootstrap、Agent/Main 启动门禁和 recovery restore 均实际运行 | 覆盖的是已执行路径，不包含崩溃注入矩阵 |
| 正常桌面人工验收 | 用户确认正常打开、正常关闭，并最终确认“我确定没问题了” | 记为 `通过（用户确认）`；Widget/主窗体逐项操作没有独立录屏或签字表 |
| 用户签字 | 未提供带版本、环境、日期的正式签字材料 | `info insufficient`，不能伪写成审计签字 |

## 5. 未关闭项目与 finding

### P275-EVID-005：P2.75-04 的全覆盖故障证据仍不足

- 裁决：`partial` / `info insufficient`。
- 严重度：`Major（证据阻塞）`。
- 是否阻塞 P2.75：是（阻塞“全部 P2.75 已完成”的表述）；不阻塞在当前隔离环境中继续开发。
- 精确依据：计划要求在 `docs/reports/P2收尾与P2.5-P2.75-P3开发计划-2026-08-29.md:157-172` 完成升级 clone、故障注入、重启、原库不变、真实进程和人工验收；契约阶段与状态字段见 `37e01232aec1e4f34d1adda33a672622a36f6d18:docs/slices/P2.75-00-contract-freeze.md`。
- 当前已关闭：成功迁移、目标重启幂等、坏 backup fail-closed、普通启动被阻断、已验证 backup 恢复、用户正常打开/关闭确认。
- 当前未关闭：
  1. 完整 P1→P2→P2.5/P3 upgrade clone 链，而不只是 P2 基线到当前 target。
  2. lock/quiescence 竞争和进程重启/崩溃窗口。
  3. backup ACL/权限、空间不足、Flush/WriteThrough 失败分类。
  4. source changed、stage partial、checkpoint busy/残余 WAL、Verify 失败、marker 写失败。
  5. atomic promote 前后崩溃及 `promote-unknown` 恢复行为。
  6. 每类失败都保留可诊断 artifact、禁止 `result=verified` 误标成功，并核对 Active hash、长度、SQLite integrity、FK、关键数据和旧 generation 不变。
- 最小行动与归属：P2.75-04/总集成窗口补齐上述隔离故障矩阵和重启证据；P2.75-01 负责 backup durability/权限/空间类，P2.75-02 负责 lock/Candidate/checkpoint/promote 类，P2.75-03 负责 marker/status/recovery 命令类，集成负责人负责跨阶段和真实进程证据。

## 6. P2.75-04 状态及最终结论

本次用户确认可以正式关闭“正常桌面人工验收”这一项，记录为：

```text
正常桌面人工验收：通过（用户确认，隔离 clone）
坏 backup 安全失败：通过
普通启动门禁：通过
已验证 backup 恢复：通过
```

但不能据此把 P2.75-04 的所有工作写成完成。严格门禁结论保持为：

- 当前集成 HEAD 的主成功路径和已执行的恢复安全失败/成功路径：`accept`。
- P2.75-04 全部验收：`partial`，仍受 `P275-EVID-005` 阻塞。
- P2.75 总体：`partial / blocked for final release claim`；在故障注入、完整升级链和重启/原库不变证据补齐前，不宣称 P2.75 全部完成。
- P3：不得仅凭本记录宣称可用；可继续开发，但应保留上述证据门槛。

本补充记录只新增本文件，未修改既有报告或项目代码、测试代码、配置和正式数据库。
