# P2 总成接管记录（ZCode 暂替 Codex 期间）

- 日期：2026-08-28
- 接管者：ZCode（GLM），暂替触发 Codex 5h 额度限制后的在途工作
- 授权：用户明确"在我手动让你停止之前，你一直操作下去，所有操作我都同意；每一步操作、修改都要保留记录，留给 codex 后续看"
- 本文件是给 Codex 回归后的交接与审查材料，持续追加；不会 push 到远端，不修改 `reviews/`、`second-review/`。

## 一、接管时的基线快照

- 分支 `codex/p0-integration`，接管时 HEAD = `e8f7d64`（test: 对齐 RANGE 待复盘分组契约）。
- Codex 聊天记录（用户提供）显示其停止点：144 项测试中最后 1 项失败
  `LiveWidgetUsesTheSameSqliteWorkspaceAfterRestart`，根因已定位为
  `TaskQueryService.ListAsync(new TaskQuery())` 把 `PlanDate = null` 翻译成
  SQL `local_date IS NULL`，真实 SQLite 上 Today 全量读取恒为空；修复已派给
  "P2-01 查询窗口"但尚未落地。
- 工作树有两批未提交改动（均为 Codex 子任务已完成但未提交的产品）：
  1. 7 个文档路径的 P2-00 契约恢复（待提交 `docs: 整合 P2-00 契约与并行记录`）；
  2. `App.xaml.cs` + `TodayFeatureRegistration.cs` 的 Main live DI 接线（待提交
     `fix: 接通 Main 真实 Task 组合`）。
- `git stash` 中保存 Codex 的"P2 总成窗口初始工作树（集成前保留）"，本次接管
  **未触碰**该 stash。
- 环境：PATH 上的 dotnet 是 x86 无 SDK 版本；项目脚本经 `resolve-dotnet.ps1`
  解析到 `C:\Program Files\dotnet\dotnet.exe`（SDK 10.0.300/10.0.400，满足
  global.json 10.0.100 + latestFeature）。

## 二、操作时间线（全部在 D:\Anime 主工作树，仅本地提交，未 push）

### 2026-08-28 晚（接管第一轮）

1. 只读盘点：git log/status、P2-00 简报、根级活文档、Today/Widget/Infrastructure
   源码、测试夹具。确认无日期查询 bug 与 Codex 诊断一致。
2. 基线门禁：`scripts/build.ps1 -Configuration Release` → 0 警告/0 错误；
   `scripts/test.ps1 -Configuration Release` → 143/145 通过，
   失败仅 `LiveWidgetUsesTheSameSqliteWorkspaceAfterRestart`（Expected 1, Actual 0，
   对应上述根因）。
3. 修复 `TaskQueryService.ListAsync`：仅当 `query.PlanDate` 有值才追加
   `local_date` 过滤；null 日期按 `TaskQuery` 契约表示全量计划读取。
4. 新增真实 SQLite 回归测试
   `QueryWithoutPlanDateReadsAllPlannedDatesFromSqlite`
   （TaskApplicationBoundaryTests）：跨三个日期建任务，`new TaskQuery()` 返回
   全部 3 条，显式日期过滤只返回该日 1 条，全程无 tracking 实体。
5. 复跑 `scripts/test.ps1 -Configuration Release`：**145/145 通过、0 警告、
   P0-07 门禁通过**（213 资源键、7 锁定项目、1 测试项目）。
6. 检查点提交（按 Codex 既定拆分，中文提交说明，含证据与剩余风险）：
   - `712fdcb` docs: 整合 P2-00 契约与并行记录（仅 7 个文档路径）
   - `3b33875` fix: 修复无日期 Task 查询返回空集（适配器 + 回归测试）
   - `da2fc4d` fix: 接通 Main 真实 Task 组合（App.xaml.cs + TodayFeatureRegistration）

### 2026-08-28 晚（接管第二轮：改期/排序模块，详见 4.1）

1. 阅读应用层语义：`UpdateAsync`（同标题+新 TimeSpec → `PlanChanged` 历史）、
   `ReorderAsync`（`SortOrderChanged` 历史）、`Task.ChangeTime` 的结果保护。
2. 实现 `TodayPageViewModel` 改期面板（Parser 驱动、面板互斥、稳定错误码透传）
   与同日期同分组排序（交换 `sort_order`、越界/跨日期拒绝）。
3. `TodayResources.xaml` 增加改期/上移/下移按钮行与改期面板（含
   AutomationProperties）。
4. 首次编译被 CA1822 拦截（3 处按钮文案属性），改依赖 `_isLive` 后 0 警告通过。
5. 扩展测试假实现（`UpdatedCommands`/`ReorderCommands`、按 `SortOrder`+Id 排序）
   并新增 4 个测试；`scripts/test.ps1 -Configuration Release` → **149/149 通过、
   0 警告、P0-07 门禁通过**。
6. 用户指示停止代码开发；模块检查点 `50e6fc8` 落盘后转入记录收尾（见第五节）。

## 三、与 Codex 决策的衔接说明

- 接管遵循 Codex 已冻结的 P2-00 契约、ADR-0016/0017 与窗口纪律：不 push、
  不碰审查材料、每个可独立验收的大模块一个中文检查点提交。
- Main live DI 接线与文档恢复两批改动**逐字保留** Codex 子任务的产出，只做了
  提交动作，未重写内容。
- 无日期查询修复按 Codex 派发给"查询窗口"的任务范围执行（适配器 + 回归测试），
  未扩大 Core API。

## 四、接管期间的后续模块（持续追加）

### 4.1 Main TODAY 改期/排序用户入口（已完成）

- 改动路径：
  - `src/windows/ReminNote.Windows/Features/Today/TodayPageViewModel.cs`：
    - 页面级改期面板（`IsRescheduleOpen`/`RescheduleDateText`/`RescheduleTimeText`/
      `RescheduleTaskTitle`、`ConfirmRescheduleCommand`/`CancelRescheduleCommand`），
      与 Quick Add 面板互斥打开；
    - 改期用冻结的 `TaskParser` 解析"日期 + 时间 + 占位标题"取 `TimeSpec`，标题
      不变，经 `ITaskApplicationService.UpdateAsync` 写入（时间变化才产生
      `PlanChanged` 历史，已有结果由领域拒绝），空输入映射 `task.parser.empty`，
      保留语法/非法输入透传稳定错误码；
    - 排序：任务级 `MoveUpCommand`/`MoveDownCommand`，仅交换同一计划日期同分组
      相邻两项的持久化 `sort_order`，跨日期/跨组/分组边界分别给稳定提示且不写库，
      写入走 `ReorderAsync`（两次单任务排序，各自过写门）；
    - `TodayTaskViewModel` 暴露 `PlanDate`/`LiveTimeSpec`/`SortOrder`/
      `IsPlanActionsVisible`（live 且无结果才显示按钮行）与改期/上移/下移命令；
      记录结果后按钮行自动隐藏。
  - `src/windows/ReminNote.Windows/Features/Today/TodayResources.xaml`：
    选中任务详情面板新增"改期/上移/下移"按钮行；页面模板新增改期面板（日期/
    时间输入框 + 确认/取消，全部带 AutomationProperties）。
  - `tests/ReminNote.Tests/TodayPageViewModelTests.cs`：假实现补
    `UpdatedCommands`/`ReorderCommands` 记录并把排序改为按 `SortOrder` 再按 Id
    （对齐生产排序契约，原有测试不受影响，默认 SortOrder=0 落回 Id 序）；新增
    4 个测试：改期成功保留标题、已完成任务拒绝改期 + 空输入/保留语法拒绝、
    同日期同分组交换 `sort_order` 且刷新后顺序生效、跨日期相邻/分组边界拒绝写库。
- 证据：`scripts/test.ps1 -Configuration Release` → **149/149 通过、0 警告、
  P0-07 门禁通过**（213 资源键不变）。
- 已知取舍（供 Codex 复核）：改期面板文案与按钮文案沿用 P2-03 既有 VM 常量
  模式未加 resx 键；排序为两次独立 `ReorderAsync`，两次之间理论上可被另一进程
  插入写门操作，最坏结果是两项同 `sort_order` 回落到时间/Id 稳定序，不会丢数据
  或越组——P2.5 Agent 单写者会整体消除该窗口。
- 检查点提交：`50e6fc8`（feat: 接通 Main TODAY 改期与分组内排序，仅 3 个文件：
  VM/XAML/测试）。
- 过程记录：首次编译被 CA1822 拦下（三个按钮文案属性未访问实例数据），改为依赖
  `_isLive` 后 0 警告通过；无其他返工。

## 五、停止点与最终状态（2026-08-28）

用户指示"停止代码的开发，完善这段时间的开发记录"，接管者据此停止编码，收尾动作
仅限记录与提交：

1. 模块 4.1 检查点已提交（`50e6fc8`），149/149 测试全绿后落盘，无未验证代码
   遗留；工作树除记录文档外干净。
2. 根级活文档已对齐事实：ARCHITECTURE（Main live 组合已接通）、PRODUCT_RULES
   （当前阶段）、DEVELOPMENT（检查点状态）、TESTING（双宿主人工验收仍未执行）、
   P2-00 简报（风险项 2/3/4 关闭、5 待执行，顶部状态行同步）。
3. 未执行的遗留项（给 Codex/用户的接续清单）：
   - Main/Widget 双宿主真实进程人工验收（TESTING.md P2 清单：重启保留、RANGE
     NEEDS REVIEW、跨午夜、非法 Parser、migration 不删库、数据路径、审查文件
     不变）；
   - 真实 `.devdata/reminnote.sqlite` 迁移幂等验证（连续两次启动观察无异常、
     数据保留）；
   - P2 阶段最终报告（docs/reports）；
   - push 决策：远端 `codex/p0-integration` 停留在 `f5be3b6`，本地已领先多个
     检查点，是否 push 由用户/Codex 回归后决定，接管者未 push。
4. `git stash`（Codex 的"P2 总成窗口初始工作树（集成前保留）"）全程未触碰；
   `reviews/`、`second-review/` 全程未修改。

### 接管期间本地提交索引（未 push）

| 提交 | 说明 |
|---|---|
| `712fdcb` | docs: 整合 P2-00 契约与并行记录（Codex 子任务产出的文档恢复） |
| `3b33875` | fix: 修复无日期 Task 查询返回空集（适配器 + 真实 SQLite 回归测试） |
| `da2fc4d` | fix: 接通 Main 真实 Task 组合（App.xaml.cs + Today DI） |
| `a17be77` | docs: 建立总成接管记录（本文件） |
| `50e6fc8` | feat: 接通 Main TODAY 改期与分组内排序（VM/XAML/测试） |
| （本次） | docs: 收尾总成接管记录与风险状态对齐 |

## 六、本轮 P2-05 最终验收与文档（2026-08-28）

本窗口基于验收时实际代码 HEAD `d6edc0e6723afaad111898f9f7848ba09e7e21cc` 独立完成最终验收材料。当前 worktree 初始状态干净；本轮不修改源码、测试、根级活文档或 `reviews/`、`second-review/`，只新增 P2-05/阶段报告并在本文件末尾追加事实。

### 6.1 指定命令证据

1. `scripts/build.ps1 -Configuration Release`：退出码 0；还原成功，Core、Infrastructure、Windows、Agent、Bootstrap、Widget、Tests、P1ManualHarness 共 8 个项目成功构建；输出无 warning/error。
2. `scripts/test.ps1 -Configuration Release`：退出码 0；xUnit v3 executable 实际 `Total: 160, Errors: 0, Failed: 0, Skipped: 0, Not Run: 0`；脚本末尾 P0-07 通过。
3. `scripts/verify-p0-07.ps1`：退出码 0；`213 resource keys, 7 locked projects, 1 test projects`，Shell/TODAY/ANIME/Widget 标记通过。
4. `git diff --check`：退出码 0，无 whitespace errors。

本机脚本解析的 dotnet 为 `C:\Program Files\dotnet\dotnet.exe`，版本 `10.0.400`；机器安装了 `10.0.300` 和 `10.0.400`。

### 6.2 真实 `.devdata` schema/migration 核对

本轮开始时 `.devdata/reminnote.sqlite` 不存在，目录只有 `.gitkeep` 和 README，没有既有开发数据可被覆盖。使用已构建的 P1ManualHarness 对当前仓库根目录执行两次 `schema`，均退出码 0，且两次均报告：

```text
表：__EFMigrationsHistory, __EFMigrationsLock, app_settings, task_history, tasks
数据库：C:\Users\EMT\.codex\worktrees\98b7\Anime\.devdata\reminnote.sqlite
```

第二次后通过只读 SQLite 连接核对：`integrity_check=ok`、`foreign_key_check` 行数为 0；migration history 为 `20260828025922_InitialTaskSchema`、`20260828120000_P2TaskLoop`、`20260828130000_P2ContinuationDeleteBoundary`；表行数为 `app_settings=1`、`task_history=0`、`tasks=0`。两次主文件 SHA256 不同，故只以两次迁移成功、schema/history 一致和只读完整性检查作为幂等证据，不声称字节 hash 一致；未执行 purge、删除或覆盖数据库。由于当前真实库为空，带已有 P1 数据的真实升级保留仍未在本窗口完成。

### 6.3 人工验收状态

本窗口无法真实点击 WPF GUI，因此以下均为“待用户执行”：

- Main/Widget 双宿主以同一个 `--repo-root` 启动、共享 `.devdata/reminnote.sqlite`；
- Main/Widget Quick Add、DONE、PARTIAL/MISSED、刷新和重启保留；
- 结束 RANGE 的 `NEEDS REVIEW` 与跨午夜次日 00:30 归属/次日结束转复盘；
- Main 改期、同组排序、`PARTIAL RANGE` 继续关系；
- 非法 Parser 不写库、无效仓库根目录非零退出。

具体启动命令、点击顺序、预期和失败判定见新增 `docs/slices/P2-05-final-acceptance-and-documentation.md` 与 `docs/reports/P2-阶段集成报告-2026-08-28.md`。

### 6.4 本轮结论

自动化和当前空开发库 schema/migration 检查通过；不能把 GUI、真实已有数据 migration、重启和跨午夜人工项写成已通过。P2 仍保持明确边界：没有 Reminder/ReminderRule/Schedule/Instance、Scheduler、Agent 业务 IPC、WAL、Change Journal、Sync 或 Anime 网络实现；现有 Main/Widget named pipe 只负责各自单实例激活，不承载业务命令。本轮文档检查点只在本地提交，不 push、不 merge、不 rebase。

## 6.5 总成最终集成与远程同步

随后由总成窗口将 P2-03/P2-04、P2 持久化/历史/迁移/写入边界，以及 Main 改期/排序字段边界等提交逐一 cherry-pick；初次代码验收/首次同步点为根提交 `b0a4321`。随后为追加同步事实记录，总成窗口 cherry-pick 本 P2-05 窗口此前的文档提交，追加同步记录点为 `27b6a77`；其后为修正该记录生成文档事实修正点 `484d78a`。`b0a4321` 之后的 `27b6a77`、`484d78a` 均只修改报告文档，代码门禁证据未改变。

总成根目录实际执行并通过：

- `scripts/build.ps1 -Configuration Release`：8 个项目，0 warning、0 error；
- `scripts/test.ps1 -Configuration Release`：160/160；
- `scripts/verify-p0-07.ps1`：213 resource keys、7 locked projects、1 test project；
- `git diff --check`：通过。

由总成窗口执行（不是本 P2-05 窗口）：`git -c http.version=HTTP/1.1 push origin HEAD:codex/p0-integration`，第一次命令成功，远程快进 `f5be3b6..b0a4321`；随后第二次 push 成功，远程快进 `b0a4321..27b6a77`；为同步文档事实修正，第三次 push 成功，远程快进 `27b6a77..484d78a`。截至第三次文档同步时，记录点为 `484d78a`；其后总成窗口又将本窗口上一文档提交 `c60b77b` cherry-pick 为 `4e44b05` 并推送，故 `484d78a` 仅是历史文档同步点，不表述为当前最终根提交或当前远程指向。`reviews/`、`second-review/` 未修改，P2 原始 stash 保留。Main/Widget GUI、重启、RANGE/跨午夜和真实已有 P1 数据 migration 仍待用户执行；本节不将其写为通过。本 P2-05 窗口此前仅提交本地文档，未执行 push。

## 6.6 真实运行验收尝试（条件性）

本节补记本窗口随后基于 `D:\Anime` 当前 Release 二进制进行的真实运行验收尝试，不改写第 6.3 节的历史验收基线；以下结果是“条件性运行 smoke”，不是用户桌面签字。

- Codex exec 默认环境中 `WINDIR` 为空、`SystemRoot=C:\WINDOWS`。Main/Widget 原始启动均出现 WPF FontCache `UriFormatException: Invalid URI`，事件日志定位到 `App.InitializeComponent` → `MS.Internal.FontCache.Util`。源代码只读分析和最小 WPF/资源加载对照表明，只有在启动子进程补齐 `WINDIR=C:\WINDOWS` 后，最小 WPF 与 ReminNote Main/Widget 才能创建窗口；这是执行环境限制，不是 ReminNote `App.xaml` 资源缺陷。
- 补齐子进程 `WINDIR` 并以 `--repo-root D:\Anime` 启动后，UI Automation 真实读到 Main `ReminNote` 与 Widget `ReminNote Widget`。Main Quick Add → Widget 轮询可见，Widget Quick Add → Main 刷新可见；两端 DONE 均写入真实 SQLite/History。
- 正确语法 `今天 22:00-22:30 ...` 进入 `NEEDS REVIEW`；Main 记录 `PARTIAL` 后，数据库只读检查确认 RANGE 边界、result 和 history。顺序重启后两端均保留 `0 OPEN · 4 DONE` 与 `PARTIAL`。
- 无效 `repo-root` 两端均非零：Widget 为 `1`，Main 为未处理启动异常码。
- 只读检查 `D:\Anime\.devdata\reminnote.sqlite`：integrity `ok`、外键检查 0、3 条 migration、4 条 UI smoke Task、4 条 History；`reviews/`、`second-review/` 无 diff，未修改源码或测试。
- Main 旧 Shell Mock 文案已由独立提交 `3276f0c` 修正为 live 文案，并已完成当前二进制 UIA 检查；该旧文案不再是当前风险。Anime P0 Mock 以及 Reminder/Agent/IPC/Sync 等 P2.5 未实现边界仍保持原记录。
- 默认 exec 环境需要补齐 `WINDIR`，所以以上只能作为条件性运行 smoke，不能替代用户桌面签字。跨午夜 `00:30` 时序、改期/排序/继续完整点击、正常桌面下的 Main/Widget GUI 与重启，以及带已有 P1 数据的真实 forward migration 仍待用户执行。一次并行重启尝试中 Widget 曾短暂退出，随后重试及顺序启动成功。

## 6.7 排序修复后二次条件性验收补记

独立窗口发现并修复 Main TODAY 同日期同分组默认 `sort_order=0` 无法交换的产品缺陷，代码与回归测试提交为 `8a86a002e3148ff58570aeba4971fc3f462ea283`。根因是旧实现把 `0↔0` 写回，应用层判定为 no-op；修复后在重复 `sort_order` 时按当前同日期同分组可见顺序归一化后再交换，同时保留跨日期、跨分组和已有结果保护。

总成在该修复后重新执行并通过 Release 门禁：build 为 8 个项目、0 warning、0 error；`scripts/test.ps1 -Configuration Release` 为 `162/162`；`scripts/verify-p0-07.ps1` 为 `213 resource keys/7 locked projects/1 test project`；`git diff --check` 通过。上述数字是修复后的重新执行结果，不改写 6.1 节首轮 `160/160` 的历史记录。

修复后二次条件性 UIA 使用 `D:\Anime` Release Main，显式传入 `--repo-root D:\Anime`，并在启动子进程时补齐 `WINDIR=C:\WINDOWS`：

- 本次仅追加两条带前缀的 Task：`P2_FINAL_SMOKE SORT_FIX2_C` 和 `P2_FINAL_SMOKE SORT_FIX2_D`。选中 C 点击“下移”后 UI 顺序为 D→C；只读 SQLite 确认 C/D `sort_order=4/3`，并存在 `task_history.kind=3` 的 `SortOrderChanged` 历史。
- 重新选中 C 点击“上移”后 UI 顺序为 C→D；最终 C/D `sort_order=3/4`，排序历史仍存在。
- 已有结果的 Task 不显示改期/排序按钮；跨日期、跨分组保护通过。Main 正常关闭，工作树、`reviews/`、`second-review/` 无变化。
- 此前反向一次未重新选中 C，导致 UI Automation 实际选中 D；这是测试选中状态问题，不是产品失败。

本次仅追加上述两条 `P2_FINAL_SMOKE` Task，未清空、删除或覆盖 `D:\Anime\.devdata\reminnote.sqlite`。当前真实开发库只读检查为 `integrity_check=ok`、`foreign_key_check` 行数 0，migration 共 3 条；这不是带已有 P1 数据的真实 forward migration 证据。默认 Codex exec 环境的 `WINDIR` 限制仍需在用户环境区分；跨午夜 `00:30`、用户正常桌面签字、真实已有 P1 数据 forward migration 仍待用户执行。

本补记继续保留 P2.5 边界：Reminder、Agent 业务 IPC、Sync 等未实现；本 P2-05 文档窗口不修改源码、测试、根级活文档、`reviews/` 或 `second-review/`，不执行 push、merge、rebase。

## 七、P2 终审整改、人工门禁与远程冻结（2026-08-29）

本节为当前总成接管状态的追加记录；此前各节的历史事实保留不改写。本节依据 P2 终审及 Parser 二审
原件与当前本地引用核对，明确区分代码/自动化证据和用户人工验收证据。

### 7.1 当前终审结论

- P2 终审为**有条件通过**：`IMP-P2-1` 已解除，但 `P2-GATE-01` 尚未由用户完成，因此不能把
  P2 写成已完成、已完全验收或已获 GUI 签字。
- Parser 修复提交为 `9a7d293e6db52d617087d888168217165cc367af`；针对性 Parser 二审报告提交为
  `9ec849352963f8306d1b0d023e25535adb2e7dbc`。Parser 粘连日期/时间守卫、Main/Widget 失败早退、
  Parser 类 40/40 及全量测试 170/170 均有报告证据。
- 当前 Release 门禁记录为 8 个项目构建、0 警告、0 错误，P0-07 通过。自动化结果不等于正常桌面
  双宿主、真实跨午夜或已有 P1 数据迁移的人工签字。
- P2-SR-01 已由 `6d3d3fff95a998a153483dc36883a338da68ff24` 澄清。所有人工验收命令和宿主都必须
  以总成根目录 `D:\Anime` 为准，使用 `--repo-root D:\Anime`；不得使用历史独立 worktree `9949`/
  `98b7`。验收必须使用测试数据库或可恢复副本，不得覆盖真实数据/生产数据，也不得 purge、删除或
  覆盖原始数据库。

### 7.2 当前人工验收清单（P2-GATE-01，仍未完成）

1. Main 与 Widget 从 `D:\Anime` 启动并共享同一验收测试/副本数据库；核对同一 SQLite 路径，完成
   双向 Quick Add、刷新、DONE、PARTIAL/MISSED 和结果/历史写入。
2. 验证结束 RANGE 的 `NEEDS REVIEW`、改期、同组排序、`PARTIAL RANGE` 继续，以及关闭/重启后的
   任务、结果、历史和顺序保留。
3. 进行真实跨午夜 `00:30`/`01:00` 时序，核对次日归属、RANGE 结束后的复盘状态和历史记录。
4. 在已有 P1 数据的副本上完成 forward migration、连续启动和幂等核对，证明数据保留且原始库不被
   覆盖。
5. 在两个宿主输入 `今天18:00`、`明天18:00`、`后天08:05` 及带标题粘连范围，确认稳定 Parser
   错误、无成功提示、Today 不出现错误标题、`tasks` 行数不增加；再验证合法空格语法
   `明天 18:00` 的日期/时间/标题和跨宿主刷新。
6. 以无效 `--repo-root` 启动 Main 与 Widget，确认非零退出并保留退出码；验收完成后核对
   `reviews/`、`second-review/` 原件无变化。

上述项目在用户给出实际路径、时间、数据库行数/历史和失败项前都保持“待用户执行”；条件性 UIA、
自动化测试和空库 schema 检查不能替代正常桌面证据。

### 7.3 P2.5 后置项

`IMP-P2-2` 与 `P2-EXTRA-01` 均记录为 P2.5 后置，不阻塞本次 P2 代码闭环：

- `IMP-P2-2`：在服务/命令层增加排序的计划日期、Today 分组、邻接项或版本上下文验证；当前
  P2 保留 Main UI 守卫。
- `P2-EXTRA-01`：将一次排序交换的多次独立 application 写入改为服务级批量命令/单事务，并补
  交错写入与失败回滚测试。

这两个后置项不构成本次代码整改阻断；真正阻塞 P2 完成声明的仍是 `P2-GATE-01` 人工证据。

### 7.4 当前本地提交与远程状态、下一步顺序

截至本轮文档追加前，`D:\Anime` 总成根目录的 `codex/p0-integration` 与本窗口基线均指向
`6d3d3fff95a998a153483dc36883a338da68ff24`；`origin/codex/p0-integration` 实际指向
`f9a8395d515feceb04ec71263e443befb5d7cc1e`，本地分支相对远程 ahead 5。本窗口使用独立 detached
worktree，本次只产生文档提交，待总成窗口整合；本轮不 push、不 merge、不 rebase、不 force-push，
远程保持冻结。

后续顺序固定为：**用户完成人工门禁 → 总成窗口核对路径与证据 → 更新 P2 完成声明**。在此之前，
不得以本地提交、条件性 smoke 或自动化结果宣告 P2 已完成。

## 八、P2-GATE-01 新验收补证与当前门禁状态（2026-08-29）

本节追加记录 `D:\Anime` 总成侧新验收报告
`docs/reports/P2-GATE-01-runtime-acceptance-2026-08-29.md` 及补证提交
`0b6d760caad7f183db2dd6dc6dd50a591f3f486e`。报告补充了隔离副本和 Codex/CLI 证据，但不解除
`P2-GATE-01`，不能把 P2 写成已完成或用户已签字。

### 8.1 Pass 与环境事件 Fail

- 隔离 Widget 的真实 Release 启动、UI Automation、RANGE `NEEDS REVIEW`、MISSED/PARTIAL、DONE、
  History、SQLite 核对及重启后的数据库保留，记录为 **Pass（Codex 实机 smoke，非用户签字）**。
- P1 forward migration、第二次正常 schema/迁移路径、旧 Task 保留，记录为 **Pass（Codex 非 GUI 补证）**；
  P1 seed 未改写，升级副本保留旧 Task，migration history、`integrity_check=ok` 和无外键违规均已核对。
- Widget 无效 `--repo-root` 的入口方法级拒绝及目标目录未创建，记录为 **Pass（非 GUI 补证）**；
  该结果不是 Widget 可执行文件退出码证据。
- 一次合法 `明天 18:00` 输入受并发键盘/UIA 干扰，实际写入错误前缀标题，记录为 **Fail（环境事件）**；
  不归因产品，也不计作合法输入通过。另一次经 UIA 值核对后的合法输入写入，只能计为带限定的
  Codex smoke。

上述所有 Codex/CLI、UI Automation、harness 和 SQLite 副本结果都不能替代用户正常桌面人工签字。

### 8.2 Pending 项与人工验收边界

以下仍为 **Pending**：

1. Main/Widget 双向 Quick Add、刷新及同一 SQLite 数据库的完整双宿主证据；当前只补齐 Widget 隔离
   smoke，未形成双向同库无竞争证据。
2. 真实 `23:00 → 次日 00:30 → 01:00` 跨午夜时序；现有 `23:00–01:00` 结构证据不能替代真实时钟
   节点观察。
3. Main 与 Widget 可执行文件在无效 `--repo-root` 下的进程级非零退出；Widget 方法级拒绝且未建目录
   的 Pass 不能替代两宿主 EXE 证据。
4. 用户正常桌面完成 Main/Widget 操作、重启/结果保留和最终人工签字；Codex 操作与 CLI 结果不构成
   用户验收。

人工验收必须从总成根目录 `D:\Anime` 执行，使用 `--repo-root D:\Anime`，并使用验收专用测试数据库
或可恢复副本；不得使用历史独立 worktree `9949`/`98b7`，不得覆盖真实/生产数据，也不得将临时 clone
数据库复制回 `D:\Anime`。

### 8.3 后置项、下一步和远程冻结

`IMP-P2-2` 与 `P2-EXTRA-01` 仍为 P2.5 后置，不阻塞本次 P2 代码闭环；`P2-GATE-01` 用户人工证据
仍阻塞 P2 完成声明。顺序保持：**用户完成人工门禁 → 总成窗口核对路径、数据库与操作证据 → 更新
完成声明**。

新报告提交后，`D:\Anime` 的 `codex/p0-integration` 指向
`0b6d760caad7f183db2dd6dc6dd50a591f3f486e`，远程 `origin/codex/p0-integration` 仍指向
`f9a8395d515feceb04ec71263e443befb5d7cc1e`，本地 ahead 8；本轮不 push、merge、rebase 或
force-push，远程保持冻结。本窗口除两份目标报告外不修改任何文件。

## 九、P2-GATE-01 双宿主复测与启动环境补记（2026-08-29）

本节追加记录验收报告补证提交 `b7b3c38927b60478f4ee9a46376135fcf41006f4` 的独立一次性 clone
复测结果；不改写前文，不解除人工门禁，也不把 Codex smoke 当作用户签字。

### 9.1 Codex smoke Pass

- Main→Widget 与 Widget→Main 在同一临时 SQLite 上完成 Quick Add/刷新互读，记录为 **Pass（Codex
  UIA smoke）**。
- RANGE `NEEDS REVIEW`、`MISSED`、`PARTIAL`、`COMPLETED`、History 和顺序重启保留均通过；只读
  核对为 `tasks=4`、`task_history=3`、`integrity_check=ok`、外键违规 0。
- 两宿主无效 `--repo-root` 的 Release EXE 真实退出且未建未知目录/数据库：Main 退出码
  `-532462766`，Widget 退出码 `1`；该项为 **Pass（Codex EXE 参数失败 smoke）**，不是用户签字。

### 9.2 环境事件与 Pending

- 首次启动因环境缺少 `WINDIR` 触发 WPF FontCache `UriFormatException`，记录为 **Fail（环境事件）**，
  不归因产品；补齐 `WINDIR=C:\WINDOWS`、`SystemRoot=C:\WINDOWS` 后 Main/Widget 启动成功。
- 前一轮并发 UIA 导致合法输入被加前缀的环境事件 Fail 继续保留，不计为合法输入通过。
- 真实 `23:00 → 次日 00:30 → 01:00` 跨午夜仍 **Pending**；用户正常桌面人工验收与最终签字仍
  **Pending**。所有 Codex UIA、CLI、harness、退出码及 SQLite 证据均不能替代用户签字。

后续人工验收必须使用总成根目录 `D:\Anime` 和 `--repo-root D:\Anime`，并使用验收专用测试库或可恢复
副本；不得覆盖真实/生产数据，不得使用历史独立 worktree `9949`/`98b7`，也不得复制临时库回总成库。
`IMP-P2-2` 与 `P2-EXTRA-01` 仍为 P2.5 后置，不阻塞本次 P2 代码闭环；下一步保持“用户完成人工门禁
→ 总成窗口核对证据 → 更新完成声明”。

当前 `D:\Anime` 的 `codex/p0-integration` HEAD 为
`8d3ac90b99e95c87e58a4716ba2f5b0500eac051`，远程仍为
`f9a8395d515feceb04ec71263e443befb5d7cc1e`，本地 ahead 10。本轮继续不 push、merge、rebase 或
force-push，远程保持冻结；本窗口仍只修改两份目标报告。

## 十、P2 用户人工验收报告与交互终审同步（2026-08-29）

本节追加记录用户报告、交互修复和独立终审事实；不重写前文，不扩写用户未提供的人工测试细节，
也不将功能分支进入合并流程写成 `main` 已合并。

### 10.1 用户报告与跨午夜门禁

2026-08-29 **用户报告**：除真实 `23:00 → 次日 00:30 → 01:00` 跨午夜时序外，`P2-GATE-01` 其余
人工测试已完成，并已让功能分支进入合并流程。这里只记录该报告，不推断未给出的具体操作、路径、
数据库行数或签字证据，不声称 `main` 或远程已合并。

真实跨午夜时序仍为 **Pending**；在用户给出结果并由总成窗口核对前，不更新 P2 完成声明。

### 10.2 交互修复与独立终审

- `4406e7d4faf0d2e3d7d342ba5eadecf18e176f83` 收敛 Widget 写入/刷新失败反馈；
  `1dc095f8c1a015d723e2fffebd7465a3958d388f` 收敛 Main 交互与刷新。
- 自动化总成证据为 Release 8/8、0 警告/错误、测试 186/186、P0-07 的 `213 resource keys/7 locked
  projects/1 test project`，以及 `git diff --check` 通过。
- 独立终审报告 `6227b0150d9ff190dba6f5d225097681eae42945` 判定 R1/R2/R4/R5 已解除；R3 主要缺口已
  修复但保留刷新并发竞态 MINOR；Parser 粘连修复仍有效。自动化/独立复核不能替代用户跨午夜门禁。
- `IMP-P2-2` 排序服务上下文和 `P2-EXTRA-01` 批量原子性继续作为 P2.5 后置，不阻塞本次 P2 代码
  整改范围。

### 10.3 Git 边界与下一步

当前 `D:\Anime` 的 `codex/p0-integration` HEAD 为
`47b0a6e2782e2d768b52b5b9e51689c0ce16a68d`，远程 `origin/codex/p0-integration` 仍为用户已推送的
`6f8e127ada7dcb843018a5d5ef7f874e0b0f4ee4`；`main` 与 `origin/main` 均为
`fb74398baebd16174b667af4730c5eef25392938`。本地仅保留未推送提交，不声称本轮提交已在远程；本轮
不 push、merge 或 rebase，远程继续冻结。

后续顺序固定为：**用户完成真实跨午夜并给出结果 → 总成核对当前 HEAD、数据库与证据 → 必要时做短
回归 → 更新 P2 完成声明**。本窗口仍只修改两份目标报告。
## 十一、P2 后续硬化与终审边界同步（2026-08-29）

本节追加记录当前总成 `216f3e9` 对 `db18a10` 终审更正意见的处理；不重写前文，不把独立窗口旧门禁
数字或尚未产生的独立 rerun 结论写成最新事实。

### 11.1 更正项与修复提交

- `db18a10a472f6c441ccf9b791bbe26fae383d961` 识别的 Main TODAY 刷新竞态，由
  `0a3df93807ec0ee54ba35b7e5ad9f0203c79aec5` 收敛；Slice 为
  `docs/slices/P2-10-today-refresh-concurrency.md`。
- `db18a10` 识别的 Widget 外部选中回退、Widget 宿主致命异常边界，由
  `216f3e90c8f16270bf70e45eb396b1b78edae9bc` 收敛；Slice 为
  `docs/slices/P2-11-widget-selection-fatal-boundary.md`。
- `db18a10` 更正报告已接入当前总成。针对 `0a3df93`/`216f3e9` 的最新独立 rerun 尚未产生新报告，
  因此不能声称独立复核已经通过；当前仅确认代码与自动化门禁已通过。

### 11.2 最新总成门禁

最新总成实际结果为：Release 构建 8/8、0 warning/error；测试 191/191，Errors、Failed、Skipped、
Not Run 均为 0；`scripts/verify-p0-07.ps1` 通过（213 resource keys、7 locked projects、1 test
project、UI markers）；`git diff --check` 通过。独立窗口旧的 180/180、186/186 数字不作为最新总成
结果。

### 11.3 用户报告与仍待完成事项

此前 `2026-08-29` 的**用户报告**是：除真实 `23:00 → 次日 00:30 → 01:00` 跨午夜外，
`P2-GATE-01` 其余人工测试已完成，并已让功能分支进入合并流程。该报告早于本轮后续硬化；不推断
用户已复测 `0a3df93`/`216f3e9`，不扩写未提供的人工证据，也不写成 `main` 已合并。

真实跨午夜时序仍 **Pending**。需要短回归或用户确认时，必须针对最新硬化后的总成执行并记录；在
用户完成真实跨午夜并由总成核对当前 HEAD、数据库和证据前，不更新 P2 完成声明。P2.5 排序服务上下文
与批量原子性继续后置。

### 11.4 Git 边界与下一步

截至本轮追加前，`D:\Anime` 的 `codex/p0-integration` HEAD 为
`216f3e90c8f16270bf70e45eb396b1b78edae9bc`，远程 `origin/codex/p0-integration` 为用户已推送的
`6f8e127ada7dcb843018a5d5ef7f874e0b0f4ee4`；`main` 与 `origin/main` 均为
`fb74398baebd16174b667af4730c5eef25392938`。本窗口新增提交仅保留本地、未 push，不能声称已提交
远程或已合并到 `main`。

下一步固定为：**用户完成真实跨午夜并给出结果 → 总成核对当前 HEAD、数据库与证据 → 必要时做短
回归 → 才能更新 P2 完成声明**。

## 十二、最新独立 rerun 复核接入与 P2 状态同步（2026-08-29）

本节只追加记录最新独立 rerun 及其总成接入状态，不改写前文，不将 Codex/CLI 证据替代用户人工签字，也不写成 P2 已最终完成。

### 12.1 独立 rerun 复核与裁决

- 最新独立 rerun 复核提交为 `83b7db3d2052d0e29551817aaa70c83a3cd2db25`，已由总成 cherry-pick 接入；总成接入提交为 `68ddd07f6a719fab905407f48b1726ffa392c716`。
- 静态复核目标为总成 HEAD `216f3e90c8f16270bf70e45eb396b1b78edae9bc`，确认 Main `0a3df93807ec0ee54ba35b7e5ad9f0203c79aec5` 的 VM 刷新串行门、代次丢弃、取消与释放边界，以及 Widget `216f3e90c8f16270bf70e45eb396b1b78edae9bc` 的外部选中失效、自有结果转移和宿主 fatal 过滤；未发现 Critical、BLOCKER 或 MAJOR。
- 对应 Slice 为 `docs/slices/P2-10-today-refresh-concurrency.md` 与 `docs/slices/P2-11-widget-selection-fatal-boundary.md`。R1/R2/R4/R5 主路径解除；R3 核心旧读模型覆盖风险解除，但命令刷新反馈仍可能有 MINOR 代次副作用。`IMP-P2-2` 与 `P2-EXTRA-01` 继续 P2.5 后置。

### 12.2 总成门禁与证据边界

最新总成此前已实际通过：Release 构建 8/8、0 warning/error；测试 191/191，Errors、Failed、Skipped、Not Run 均为 0；P0-07 通过，包含 213 resource keys、7 locked projects、1 test project；`git diff --check` 通过。上述为总成实跑事实，独立 rerun 本身未重跑这些门禁，不得写成独立 rerun 的重跑结果。

### 12.3 用户报告与人工门禁

此前的**用户报告**为：除真实 `23:00 → 次日 00:30 → 01:00` 跨午夜时序外，其余人工测试已完成，并已让功能分支进入合并流程。该报告发生在后续硬化之前；不推断用户已复测 `0a3df93`/`216f3e9`，不扩写未提供的操作、路径、数据库或签字细节。

真实 `23:00 → 次日 00:30 → 01:00` 跨午夜人工门禁仍 **Pending**，P2 完成声明仍阻塞。独立 rerun、Codex/CLI、harness 或总成自动化证据不能替代用户人工确认与最终签字。

### 12.4 Git 边界与下一步

截至本轮文档追加前的实际检查，`D:\Anime` 的 `codex/p0-integration` HEAD 为 `68ddd07f6a719fab905407f48b1726ffa392c716`；`origin/codex/p0-integration` 为 `6f8e127ada7dcb843018a5d5ef7f874e0b0f4ee4`；`main` 与 `origin/main` 均为 `fb74398baebd16174b667af4730c5eef25392938`。本独立文档 worktree 以既有文档提交 `26a23226aa398902e23f1cb1a72d442851ddcac7` 为基线，本轮新增提交仅在本地、未 push，不能写成已合并到 `main` 或已提交远程。

下一步固定为：**用户对最新硬化后的必要短路径做确认并完成真实跨午夜 → 总成核对当前 HEAD、数据库与证据 → 必要时做短回归 → 最后再更新 P2 完成声明**。在此之前远程继续冻结。

## 十三、额度恢复后的总成门禁复核（2026-08-29）

本节记录额度恢复后在当前总成树执行的实际门禁，不改写前述用户人工验收状态，也不把自动化结果当作跨午夜人工签字。

### 13.1 当前总成实际证据

- `./scripts/build.ps1 -Configuration Release`：通过，8/8 项目，0 warning、0 error。
- `./scripts/test.ps1 -Configuration Release`：通过，`ReminNote.Tests` 191/191，Errors、Failed、Skipped、Not Run 均为 0。
- `./scripts/verify-p0-07.ps1`：通过，213 resource keys、7 locked projects、1 test project，Shell/TODAY/ANIME/Widget UI markers 均通过。
- `git diff --check`：通过；执行后工作树干净。

上述命令是在文档整合提交 `0571c6a` 后实际执行。`0571c6a` 与前一代码硬化提交之间仅有文档整合，因此代码门禁覆盖当前总成代码；最新独立 rerun 的静态复核边界仍以第十二节和 `second-review/review-2026-08-29-p2-final-hardening-rerun.md` 为准。

### 13.2 Git 边界与人工验收状态

当前 `codex/p0-integration` HEAD 为 `0571c6a`（完整提交可由 `git rev-parse HEAD` 核对），相对
`origin/codex/p0-integration` `6f8e127` 为本地领先 10 个提交；`main` 与 `origin/main` 仍为
`fb74398`。本轮未 push、未直接合并 `main`。

用户此前报告为：除真实 `23:00 → 次日 00:30 → 01:00` 外，其余 P2-GATE-01 人工测试已完成；该报告发生在 `0a3df93`/`216f3e9` 硬化之前，因此当前记录不扩写为新硬化后的完整人工签字。真实跨午夜仍 Pending，P2 完成声明仍保持阻塞。

下一步固定为：**用户在最新总成上完成受硬化影响的必要短路径复核与真实跨午夜 → 总成核对数据库、Task 状态、历史和当前 HEAD → 更新 P2-GATE-01 与 P2 完成声明**。在此之前远程继续冻结。

## 十四、P2 收尾标记与后续阶段计划（2026-08-29）

P2 当前正式标记为：**代码闭环完成，自动化门禁通过，人工跨午夜门禁 Pending，最终完成声明暂不关闭**。用户已经报告除跨午夜外的人工测试完成；由于该报告早于最新交互硬化，不把它扩写为最新 HEAD 的完整签字。真实跨午夜时序保持待用户有可用时段后补证，不修改系统时钟、不用固定时间冒充人工验收。

P2.5、P2.75、P3 的详细目标、Slice、依赖、并行窗口、单写者切换顺序和验收标准已单独记录在：[P2 收尾与 P2.5 / P2.75 / P3 开发计划](P2收尾与P2.5-P2.75-P3开发计划-2026-08-29.md)。该文件作为后续窗口的执行约束；下一步先派发 P2.5-00 契约冻结提案，契约审核通过后再并行派发基础实现。
