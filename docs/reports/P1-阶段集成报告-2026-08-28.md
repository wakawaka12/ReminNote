# ReminNote P1 阶段集成报告

- 日期：2026-08-28
- 基线：P0 已验收；P1 集成分支基于 `fb74398`
- 当前状态：代码集成与自动化验证已完成，等待用户手动验收和一审；不得据此自动合并 `main` 或进入 P2

## 已集成模块

- P1-00：冻结 `.devdata/reminnote.sqlite`、显式 `-PurgeDevData`、P0 Mock 保留、P1 不建 Reminder/Anime/Sync 表；统一 Noda Time、EF Core、SQLite、xUnit 依赖和 lock 文件。
- P1-01：真实 Task Core、UUID v7、ANYTIME/TIME/RANGE、LocalDate/LocalTime、跨午夜和最小结果模型。
- P1-02：EF Core SQLite DbContext、真实 migration、Task 映射、schema constraints、repository 和开发库路径边界。
- P1-03：`tests/ReminNote.Tests` 领域与持久化测试、独立 `:memory:` 夹具和测试锁文件。
- P1-04：Task application service 和 query service；写入经过 application boundary，查询使用 AsNoTracking 并返回 Core snapshot。
- P1-06：合入 `tools/ReminNote.P1ManualHarness`，作为不接入 P0 UI 的开发验收 CRUD 入口。
- P1-05：P1 Slice、手动验收和数据安全/migration 材料已按实际代码补充状态说明。

## 数据库与写入路径

真实 migration 为 `20260828025922_InitialTaskSchema`，只创建 `tasks` 及 EF Core 系统表 `__EFMigrationsHistory`、`__EFMigrationsLock`。没有 Reminder、Anime、Sync 表，也没有跨午夜冗余布尔列。自动化夹具不触碰 `.devdata`；真实开发库只能通过显式 repository root 解析到 `.devdata/reminnote.sqlite`。

P1 的本地写入路径为 `P1ManualHarness -> TaskApplicationService -> ITaskRepository -> EF Core DbContext`。P0 Mock 仍为进程内展示数据，尚未接入真实 Task UI；P1 不引入 Agent IPC、Named Pipe 或 Single Writer。

## 提交与自动化证据

本地集成提交包括：

- `a41f49d`：P1-00 公共边界与依赖冻结；
- `eaba018`：测试框架基线；
- `df2f4da`：P1-05 文档材料；
- `c70463f`、`b9fc3da`：领域测试；
- `93f5f4d`：Task Core；
- `bb22df7`：测试工程、Solution、脚本和门禁接入；
- `fdaa59d`：SQLite persistence；
- `75cedba`：SQLite persistence 测试；
- `a61f4b2`：持久化测试 lock 文件；
- `cd4c2b2`：application/query boundary。
- `7b116ef`：开发验收 harness、Solution 登记和手动入口说明。

已通过：

```text
dotnet restore ReminNote.sln --locked-mode
dotnet build ReminNote.sln --configuration Release --no-restore
```

Release 构建为 8 个项目、0 警告、0 错误。`scripts/test.ps1 -Configuration Release` 当前执行 61 个 xUnit v3 executable 测试，61 通过、0 失败、0 跳过；P0-07 门禁检查 213 个资源键、7 个受其扫描范围约束的锁定项目和 P0 UI 标记通过。harness 另以临时仓库根目录完成一次黑盒 schema/CRUD smoke，结果 PASS。

## 未完成与用户验收边界

1. 真实 P1 CRUD 尚未接入 `scripts/run.ps1` 启动的 P0 WPF Mock；可通过已合入的 P1 harness 手动验证，因此 P0 Quick Add 仍不能作为真实 Task Create 证据。
2. 用户仍需在确认无真实数据的 `.devdata` 上手动执行备份、空库 migration、CRUD/重启、非法输入和恢复演练；自动化 `:memory:` 测试不能替代人工验收。
3. 用户一审应特别检查 migration 破坏性 Down、路径隔离、P0 Mock 与真实 Task 语义是否被误混，以及是否越界引入 Reminder/Anime/Sync/IPC。

详见 [P1 手动验收步骤](../slices/P1-manual-test.md)、[P1-06 集成说明](../slices/P1-06-integration-and-acceptance-brief.md) 和 [数据安全/migration 说明](../slices/P1-data-safety-and-migration-draft.md)。
